const stats = [
  {
    eat: 100,
    fun: 50,
    strength: 100,
    energy: 50,
  },

];

const buttons = ['🥗', '🍦', '⚽️', ' 🎱', '🏃💨', '⚔️', '😴', '🛌'];

const getStats = () => stats;

const getButtons = () => buttons;

export default { getStats, getButtons };
